use super::super::super::error::ProtoError;
use std::path::PathBuf;

pub(crate) fn commit_folder(
    folder: &super::ast::Folder,
    out_folder_path: &PathBuf,
) -> Result<(), ProtoError> {
    todo!()
}
